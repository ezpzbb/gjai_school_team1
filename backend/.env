# 서버 설정
PORT=3001
NODE_ENV=development

# 데이터베이스 설정 (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=wlqrkrhtlvek1!
DB_NAME=new_schema

# JWT 설정
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# CORS 설정
CORS_ORIGIN=http://localhost:3001
CORS_CREDENTIALS=true

